/**
 * Loading component for the authentication success page.
 * Renders while the success page is loading.
 */

// In Next.js App Router, a loading.tsx file creates a loading UI
// that is shown while the page or its data is being loaded
// This is part of Next.js's automatic loading state feature
export default function Loading() {
  // Returning null means no loading UI will be shown
  // You could replace this with a spinner or skeleton UI
  return null
}

